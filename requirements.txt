streamlit>=1.37
supabase>=2.6
python-dotenv>=1.0
requests>=2.32

# Gemini (Ghanwa) — unified google-genai SDK (chat, tool calling, native
# STT/TTS, embeddings for RAG). Replaces the older google-generativeai.
google-genai
pydantic-settings

# STT/TTS are handled by Gemini directly (core/stt.py, core/tts.py) — no
# separate STT/TTS provider needed. TTS output is transcoded to MP3 via
# pydub, which needs ffmpeg installed and on PATH (Windows:
# `winget install --id Gyan.FFmpeg -e`, then restart your terminal).
pydub
# pydub depends on the stdlib `audioop` module, removed in Python 3.13+.
# This restores it. Only actually needed on Python 3.13+, but harmless
# to always install.
audioop-lts; python_version >= "3.13"

# RAG / document Q&A (Ghanwa)
pymupdf
python-docx

# Voice capture widget (mic input in the browser, fed into core.stt)
audio-recorder-streamlit>=0.0.10

# Face recognition login (Rabia)
# NOTE: face_recognition wraps dlib, which needs CMake + a C++ build
# toolchain to compile on Windows. Easiest paths: `conda install -c
# conda-forge dlib` first, then `pip install face_recognition`; or
# install "Desktop development with C++" via Visual Studio Build Tools.
face_recognition>=1.3
numpy>=1.26
# face_recognition_models isn't reliably pulled in by the above on newer
# pip -- install separately if `import face_recognition` complains it's
# missing:
#   pip install git+https://github.com/ageitgey/face_recognition_models
# That package (unmaintained) imports pkg_resources, which very new
# setuptools (81+) no longer ships. This pin keeps it working:
setuptools<81

# Tests
pytest
pytest-asyncio
